security:
#password cf client will use when interacting with broker
  user.password: ${USER_PASSWORD:}

h2o:
  provisioner:
    url: localhost
    memory: 512m
    nodes: 1
    timeout: 120

yarn.config:


nats:
  url: localhost
  serviceCreationTopic:

cf:
  #name of service in cloud foundry catalog
  servicename:
  #id of service in cloud foundry catalog
  serviceid:
  baseid: 2a5f10ee-64b1-4dcb-9bf2-c8a0f12eb21b

metadata:
  imageUrl: ${IMAGE_URL:}

logging:
  level:
    water: DEBUG
    org.trustedanalytics: INFO
